poetry run pytest

poetry run pytest --cov=app --cov-report=term-missing

poetry run pytest --cov=app --cov-report=html

# только модульный
poetry run pytest -m "not integration and not e2e"

# только интеграционные
poetry run pytest -m integration

# только сквозные(требуют запущенного приложения)
# Предварительно запустить backend и frontend:
# docker compose -f infra/docker-compose.yml up -d backend postgres redis
# cd frontend && npm run dev

poetry run pytest -m e2e

# Все модульные тесты
poetry run pytest tests/unit/

# Все интеграционные тесты
poetry run pytest tests/integration/

# Все E2E тесты
poetry run pytest tests/e2e/


# Все тесты из файла аутентификации
poetry run pytest tests/integration/test_auth_api.py

# Все тесты сервиса корзины
poetry run pytest tests/unit/test_cart_service.py

# Конкретная тестовый класс
poetry run pytest tests/integration/test_auth_api.py::TestAuthAPI

# Конкретная функция
poetry run pytest tests/integration/test_auth_api.py::TestAuthAPI::test_register_success

# Все тесты, содержащие "cart" в имени
poetry run pytest -k "cart"

# Все тесты регистрации
poetry run pytest -k "register"

# Все тесты корзины ИСКЛЮЧАЯ тесты наличия
poetry run pytest -k "cart and not out_of_stock"




poetry add --group dev playwright pytest-playwright
poetry run playwright install chromium

